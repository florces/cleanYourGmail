🇪🇸 Este TikTok muestra un código de Google Apps Script que permite leer todos los correos sin leer de Gmail, incluso cuando son más de 500. El script procesa los correos en bloques, revisa cada mensaje, registra el remitente y marca los hilos como leídos automáticamente.

🇬🇧 This TikTok shows a Google Apps Script that reads all unread Gmail emails, even when there are more than 500. The script processes emails in batches, checks each message, logs the sender, and automatically marks threads as read.
